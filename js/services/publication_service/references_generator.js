window.referencesGenerator = (() => {

    function generateReferencesHTML(stats, commonData) {
        return `<h3 id="references_main">References</h3>`;
    }

    return Object.freeze({
        generateReferencesHTML
    });

})();
